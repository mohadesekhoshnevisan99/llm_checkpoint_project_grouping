# ============================================================================
# POST-DICTION — NOT A SEALED PREDICTION. The GCP hardware ran FIRST
# (2026-08-10 shakedown); this mirror was written AFTER, from its logs.
# Protocol: EXP2_SPEC.md §6 / CL-018 / CL-019; format = exp2cl_seal.yaml.
# See gcp_shakedown_mirror_f1.yaml for the full provenance ledger (shared
# setup: 2-node A100 job A + donor B, 60 s cadence, 2 GiB shard, window 360 s).
# ============================================================================
# CELL F3 — node_loss on job A's rank-0 node at t = 120 s; DRAM + SSD wiped,
# harness restored from the PEER tier on potluck-ctrl in 21.648 s
# (2 GiB over the 20.1 Gbit/s fabric + pd-bound spool write).
simulation: {seeds: [0], max_sim_s: 400.0, engine: event}
cluster:
  node_count: 3
  gpu_cpu_bandwidth_gbps: 5.2          # UNMEASURED-on-GCP placeholder (see f1)
  network_bandwidth_gbps: 2.5125      # MEASURED 20.1 Gbit/s VM-to-VM (GB/s units)
  local_ssd_bandwidth_gbps: 0.161     # MEASURED pd seq write (PROVISIONAL)
  object_store_bandwidth_gbps: 8.0
classes:
  A:
    count: 1
    ranks: 2
    cohorts: 2
    checkpoint_gb_per_rank: 1.073741824
    iteration_seconds: 0.3325          # MEASURED F3 DDP wall median, n=963
    gradient_gb_per_rank: 0.0
    iterations: 100000
    checkpoint_every: 180              # round(60 s / 0.3325 s)
    kpeers: 1
    rpo_s: 60.0
  B:
    count: 1
    ranks: 1
    cohorts: 1
    checkpoint_gb_per_rank: 1.073741824
    iteration_seconds: 60.0
    gradient_gb_per_rank: 0.0
    iterations: 100000
    checkpoint_every: 100000
    kpeers: 0
    rpo_s: 60.0
failures:
  per_node_per_second: 0.0
  weights: {process: 1.0, node: 0.0, spot: 0.0}
  restart_seconds: {process: 5.0, node: 20.0, spot: 30.0}
  trace_file: scenarios/gcp_shakedown_trace_f3.yaml
  trace_window_s: 360.0
  trace_node_map: {node30: A0, node31: A0}
  # Frozen measured constants — block2/F3/exp2_constants_measured.yaml,
  # rehearsal 2026-08-10T20:09:45Z. POST-DICTION (post-hoc by protocol).
  trace_constants:
    detect_s: 2.591
    reprovision_delay_s: 9.011
    restore_s: 21.648                  # peer-tier restore: fabric + pd write
    restore_by_class: {node_loss: 21.648}   # CL-019
store: {in_gbps: 500.0, out_gbps: 500.0, disk_gbps: 500.0, stream_gbps: 2.5125}
controller: {slot_period_s: 60.0}
arms: {ours: {kpeers: true}}
