# POST-DICTION artifact (NOT a sealed prediction — hardware ran first, 2026-08-10).
# GCP A100 shakedown mirror, cell F1 (process_kill): ONE injected kill at
# t = 120 s into the 360 s rehearsal window, target = job A's rank-0 node
# (harness label node30 = potluck-g1, a2-highgpu-1g).
# Source of truth: checkpointing/realgpu_coefficient/results/block2_shakedown_gcp/
#   block2/F1/{summary.json,supervisor.jsonl}.
provenance:
  label: POST-DICTION
  hardware_run_utc: "2026-08-10"
  source: block2_shakedown_gcp/block2/F1
  window_s: 360.0
events:
  - {t_s: 120.0, type: process_kill, target: node30}
